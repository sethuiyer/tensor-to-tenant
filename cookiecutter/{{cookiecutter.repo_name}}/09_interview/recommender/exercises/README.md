# Recommender exercises (optional)

Implement one small artifact per reading block. Keep inputs deterministic and
include a test plus a short note on the seam the implementation does not
cover yet.

- Normalize vectors and implement cosine similarity + length-weighted pooling.
- Build a toy tool graph with hop decay and weighted Adamic–Adar.
- Add smoothed IDF and a type-affinity boost to lexical matching.
- Combine signals, learn one weight from telemetry, and evaluate Recall@K/MRR/NDCG.
- Translate one query into Snowflake SQL and document permission/credit assumptions.
