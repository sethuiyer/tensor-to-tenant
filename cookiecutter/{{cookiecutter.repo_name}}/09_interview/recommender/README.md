# Tool Recommendation Systems

This is an optional, paired deep-dive that runs alongside the 108-week
spine. Read the artifacts in order:

1. **SEARCH.md** — Level 2 theory: semantic similarity, graph traversal,
   hop decay, Adamic–Adar, IDF, type affinity, dynamic thresholds,
   multi-signal ranking, telemetry, and evaluation.
2. **SNOWFLAKE.md** — Level 3 production: the same equations in Snowflake
   SQL with `AI_EMBED`, recursive CTE cycle prevention, Cortex Search,
   and Streams + Tasks.

Do not start with the SQL: it assumes the theory. Tick off sections in
`progress.md`, then capture one production failure mode or operational
trade-off per section. The exercises are optional implementation prompts.

**Pair:** theory → production · **Tier:** optional · **Track:** recommender
